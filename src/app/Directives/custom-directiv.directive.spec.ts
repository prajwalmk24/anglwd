import { CustomDirectivDirective } from './custom-directiv.directive';

describe('CustomDirectivDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomDirectivDirective();
    expect(directive).toBeTruthy();
  });
});
