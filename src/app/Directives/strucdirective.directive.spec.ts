import { StrucdirectiveDirective } from './strucdirective.directive';

describe('StrucdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new StrucdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
